#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World! Your Jenkins setup seems to work. \nThis build happend after a simple git push. \n");
   return 0;
}
