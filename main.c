#include <stdio.h>
int main()
{
   // printf() displays the string inside quotation
   printf("Hello, World! Your Jenkins setup seems to work. \n This build happend after a simple git push. Yeah \n");
   return 0;
}
